{
  "name": "autoapply-automation-server",
  "version": "1.0.0",
  "description": "Playwright-based job application automation server",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "install-browsers": "npx playwright install chromium --with-deps"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "playwright": "^1.44.0"
  },
  "engines": {
    "node": ">=18"
  }
}
